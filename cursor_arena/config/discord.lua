--[[
    Server-only. Do NOT add this file to fxmanifest files{} —
    the webhook URL must never reach a client.
]]

ArenaDiscord = {
    enabled = false,
    url = '',
    name = 'Arena',
    avatar = '',
    ffa = true,
    tdm = true,
    showdown = true,
}
